[build-system]
requires = ["maturin>=1.7"]
build-backend = "maturin"

[project]
name = "pyo3-demo"
version = "0.1.0"
description = "PyO3 demo module built from Rust"
readme = "README.md"
requires-python = ">=3.9"

[tool.maturin]
module-name = "pyo3_demo"

