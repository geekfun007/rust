# Rust HTTP API 实战项目说明

## 项目概述

这是一个完整的 Rust Web API 实战项目，展示了如何使用现代 Rust 技术栈构建一个生产级的 RESTful API 服务。

项目位置: `/workspace/rust-http-api-demo/`

## 项目特点

### 1. 完整的三层架构

```
┌─────────────────────────────────────────┐
│         HTTP Layer (Handlers)           │  ← 处理 HTTP 请求/响应
├─────────────────────────────────────────┤
│      Business Logic (Services)          │  ← 业务逻辑、验证
├─────────────────────────────────────────┤
│    Data Access Layer (Repository)       │  ← 数据库操作
├─────────────────────────────────────────┤
│           Database (SQLite)             │  ← 数据存储
└─────────────────────────────────────────┘
```

### 2. 核心技术栈

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| Web 框架 | Axum 0.7 | 基于 Tokio 的高性能 Web 框架 |
| ORM | SQLx 0.7 | 异步、编译时类型检查的 SQL 工具 |
| 数据库 | SQLite | 轻量级嵌入式数据库 |
| 异步运行时 | Tokio 1.35 | Rust 异步生态核心 |
| 序列化 | Serde 1.0 | JSON 序列化/反序列化 |
| 日志 | Tracing 0.1 | 结构化异步日志 |
| 错误处理 | 自定义 + anyhow | 统一错误处理 |
| HTTP 客户端 | Reqwest 0.11 | 异步 HTTP 客户端 |

## 项目结构详解

```
rust-http-api-demo/
│
├── Cargo.toml              # 项目配置文件
│   ├── 依赖管理
│   ├── 构建配置
│   └── 元数据
│
├── .env                    # 环境变量配置
│   ├── DATABASE_URL        # 数据库连接
│   ├── RUST_LOG           # 日志级别
│   └── SERVER_HOST/PORT   # 服务器配置
│
├── src/
│   │
│   ├── main.rs            # 应用程序入口
│   │   ├── 初始化日志系统
│   │   ├── 创建数据库连接池
│   │   ├── 运行数据库迁移
│   │   ├── 组装依赖注入
│   │   ├── 配置路由和中间件
│   │   └── 启动 HTTP 服务器
│   │
│   ├── models/            # 数据模型层
│   │   ├── mod.rs         # 模块导出
│   │   ├── user.rs        # 用户模型
│   │   │   ├── User           # 数据库实体
│   │   │   ├── CreateUserRequest  # 创建请求 DTO
│   │   │   └── UpdateUserRequest  # 更新请求 DTO
│   │   └── error.rs       # 错误类型
│   │       ├── AppError       # 应用错误枚举
│   │       └── AppResult      # Result 类型别名
│   │
│   ├── db/                # 数据访问层 (DAL)
│   │   ├── mod.rs
│   │   ├── pool.rs        # 数据库连接池管理
│   │   │   ├── create_pool()      # 创建连接池
│   │   │   └── run_migrations()   # 运行迁移
│   │   └── user_repository.rs  # 用户 Repository
│   │       ├── create()        # 创建用户
│   │       ├── find_by_id()    # 按 ID 查找
│   │       ├── find_all()      # 查询列表
│   │       ├── update()        # 更新用户
│   │       ├── delete()        # 删除用户
│   │       └── count()         # 统计数量
│   │
│   ├── services/          # 业务逻辑层
│   │   ├── mod.rs
│   │   └── user_service.rs  # 用户服务
│   │       ├── create_user()   # 创建用户（带验证）
│   │       ├── get_user()      # 获取用户
│   │       ├── list_users()    # 用户列表
│   │       ├── update_user()   # 更新用户
│   │       ├── delete_user()   # 删除用户
│   │       └── count_users()   # 统计用户
│   │
│   ├── handlers/          # HTTP 处理器层
│   │   ├── mod.rs
│   │   ├── health_handler.rs  # 健康检查
│   │   │   └── health_check()
│   │   └── user_handler.rs    # 用户 API 处理器
│   │       ├── create_user()   # POST /api/users
│   │       ├── get_user()      # GET /api/users/:id
│   │       ├── list_users()    # GET /api/users
│   │       ├── update_user()   # PUT /api/users/:id
│   │       └── delete_user()   # DELETE /api/users/:id
│   │
│   └── middleware/        # 中间件
│       ├── mod.rs
│       ├── logging.rs     # 日志中间件
│       │   └── 记录请求/响应、耗时
│       └── request_id.rs  # 请求 ID 中间件
│           └── 为每个请求生成唯一 ID
│
├── examples/              # 示例代码
│   └── client.rs         # HTTP 客户端示例
│       ├── 健康检查
│       ├── CRUD 操作演示
│       ├── 错误处理示例
│       └── 并发请求示例
│
└── tests/                # 集成测试
    └── integration_test.rs
        ├── 测试健康检查
        ├── 测试用户创建
        └── 测试验证错误
```

## 关键设计模式

### 1. Repository 模式（DAL）

将数据库访问逻辑封装到 Repository 中：

```rust
pub struct UserRepository {
    pool: SqlitePool,
}

impl UserRepository {
    pub async fn create(&self, req: &CreateUserRequest) -> AppResult<User> {
        sqlx::query_as::<_, User>(
            "INSERT INTO users (...) VALUES (...) RETURNING *"
        )
        .bind(&req.username)
        // ...
        .fetch_one(&self.pool)
        .await?
    }
}
```

**优点：**
- 数据访问逻辑集中管理
- 便于测试（可以 mock）
- 易于切换数据库实现

### 2. Service 模式

将业务逻辑与数据访问分离：

```rust
pub struct UserService {
    repository: UserRepository,
}

impl UserService {
    pub async fn create_user(&self, req: CreateUserRequest) -> AppResult<User> {
        // 1. 验证
        req.validate()?;
        
        // 2. 业务规则检查
        if self.repository.find_by_username(&req.username).await.is_ok() {
            return Err(AppError::ValidationError("用户名已存在".into()));
        }
        
        // 3. 调用 Repository
        self.repository.create(&req).await
    }
}
```

**优点：**
- 业务逻辑清晰
- 可复用的验证和规则
- 便于单元测试

### 3. 错误处理模式

统一的错误类型和转换：

```rust
pub enum AppError {
    DatabaseError(sqlx::Error),
    NotFound(String),
    ValidationError(String),
    // ...
}

// 自动转换
impl From<sqlx::Error> for AppError {
    fn from(error: sqlx::Error) -> Self {
        match error {
            sqlx::Error::RowNotFound => AppError::NotFound("记录未找到".into()),
            _ => AppError::DatabaseError(error),
        }
    }
}

// 转换为 HTTP 响应
impl IntoResponse for AppError {
    fn into_response(self) -> Response {
        let (status, message) = match self {
            AppError::NotFound(_) => (StatusCode::NOT_FOUND, self.to_string()),
            // ...
        };
        (status, Json(json!({"error": message}))).into_response()
    }
}
```

### 4. 依赖注入

使用 Axum 的状态管理实现依赖注入：

```rust
// 创建依赖
let pool = create_pool(&database_url).await?;
let repository = UserRepository::new(pool);
let service = UserService::new(repository);

// 注入到路由
let app = Router::new()
    .route("/api/users", post(create_user))
    .with_state(service);  // 注入服务

// 在处理器中使用
async fn create_user(
    State(service): State<UserService>,  // 自动注入
    Json(req): Json<CreateUserRequest>,
) -> AppResult<Json<User>> {
    let user = service.create_user(req).await?;
    Ok(Json(user))
}
```

## API 端点详解

### 1. 健康检查

```
GET /health
```

**用途：** 监控服务状态（用于 Kubernetes liveness probe）

### 2. 用户管理 API

#### 创建用户
```
POST /api/users
Content-Type: application/json

{
  "username": "johndoe",
  "email": "john@example.com",
  "full_name": "John Doe"
}
```

#### 获取用户列表
```
GET /api/users?page=1&page_size=10
```

#### 获取单个用户
```
GET /api/users/{id}
```

#### 更新用户
```
PUT /api/users/{id}
Content-Type: application/json

{
  "username": "johndoe_updated"
}
```

#### 删除用户
```
DELETE /api/users/{id}
```

## 中间件说明

### 1. 日志中间件

记录每个请求的详细信息：
- HTTP 方法
- 请求路径
- 响应状态码
- 处理耗时

```rust
async fn logging_middleware(request: Request, next: Next) -> Response {
    let start = Instant::now();
    tracing::info!("收到请求: {} {}", method, uri);
    
    let response = next.run(request).await;
    
    tracing::info!("响应完成: {} - {} ({:?})", 
        method, status, duration);
    
    response
}
```

### 2. 请求 ID 中间件

为每个请求生成唯一标识：
- 便于追踪和调试
- 添加到响应头 `X-Request-ID`

```rust
async fn request_id_middleware(mut request: Request, next: Next) -> Response {
    let request_id = Uuid::new_v4().to_string();
    request.extensions_mut().insert(request_id.clone());
    
    let mut response = next.run(request).await;
    response.headers_mut().insert("X-Request-ID", ...);
    
    response
}
```

## 数据库设计

### Users 表

```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT NOT NULL UNIQUE,
    email TEXT NOT NULL UNIQUE,
    full_name TEXT,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

**索引：**
- PRIMARY KEY on `id`
- UNIQUE on `username`
- UNIQUE on `email`

## 运行指南

### 1. 开发环境运行

```bash
cd /workspace/rust-http-api-demo

# 安装依赖并运行
cargo run

# 或使用 watch 模式（需要安装 cargo-watch）
cargo install cargo-watch
cargo watch -x run
```

### 2. 运行示例客户端

在新的终端中：

```bash
cd /workspace/rust-http-api-demo
cargo run --example client
```

这将演示所有 API 端点的使用。

### 3. 运行测试

```bash
# 先启动服务器
cargo run

# 在另一个终端运行测试
cargo test
```

## 性能优化建议

### 1. 连接池配置

```rust
SqlitePoolOptions::new()
    .max_connections(5)        // 最大连接数
    .acquire_timeout(Duration::from_secs(3))
    .connect(database_url)
```

### 2. 编译优化

```toml
[profile.release]
opt-level = 3              # 最大优化
lto = true                 # 链接时优化
codegen-units = 1          # 单个代码生成单元
```

### 3. 使用 release 模式

```bash
cargo build --release
./target/release/rust-http-api-demo
```

性能提升：10x - 100x

## 扩展建议

### 1. 切换到 PostgreSQL

```toml
[dependencies]
sqlx = { version = "0.7", features = [
    "runtime-tokio",
    "postgres",  # 改为 postgres
    "macros",
] }
```

```rust
// 更新连接字符串
let database_url = "postgres://user:pass@localhost/dbname";
```

### 2. 添加 Redis 缓存

```toml
[dependencies]
redis = { version = "0.24", features = ["tokio-comp"] }
```

```rust
pub struct UserService {
    repository: UserRepository,
    cache: redis::Client,  // 添加缓存
}
```

### 3. 添加 JWT 认证

```toml
[dependencies]
jsonwebtoken = "9.2"
```

```rust
// 创建认证中间件
async fn auth_middleware(request: Request, next: Next) -> Result<Response> {
    // 验证 JWT token
}
```

### 4. 添加 OpenAPI 文档

```toml
[dependencies]
utoipa = "4.1"
utoipa-swagger-ui = "6.0"
```

```rust
#[utoipa::path(
    post,
    path = "/api/users",
    responses(
        (status = 201, description = "用户创建成功", body = User)
    )
)]
async fn create_user(...) { }
```

## 生产部署清单

- [ ] 使用环境变量管理配置
- [ ] 启用生产级日志（JSON 格式）
- [ ] 配置数据库连接池
- [ ] 添加健康检查端点
- [ ] 配置 CORS 策略
- [ ] 启用 HTTPS（反向代理）
- [ ] 添加速率限制
- [ ] 实现认证授权
- [ ] 添加监控和告警
- [ ] 数据库备份策略
- [ ] 使用容器化部署（Docker）

## 常见问题

### Q: 如何更改数据库？

A: 修改 `.env` 文件中的 `DATABASE_URL`，然后重新运行数据库迁移。

### Q: 如何添加新的 API 端点？

A: 按照以下步骤：
1. 在 `models/` 中定义数据模型
2. 在 `db/` 中添加 Repository 方法
3. 在 `services/` 中添加业务逻辑
4. 在 `handlers/` 中添加 HTTP 处理器
5. 在 `main.rs` 中注册路由

### Q: 如何处理文件上传？

A: 使用 `axum::extract::Multipart`：

```rust
use axum::extract::Multipart;

async fn upload_file(mut multipart: Multipart) {
    while let Some(field) = multipart.next_field().await? {
        let name = field.name().unwrap().to_string();
        let data = field.bytes().await?;
        // 处理文件
    }
}
```

## 学习资源

- **Axum 官方文档**: https://docs.rs/axum/
- **SQLx 官方文档**: https://docs.rs/sqlx/
- **Tokio 教程**: https://tokio.rs/tokio/tutorial
- **Rust 异步编程**: https://rust-lang.github.io/async-book/

## 总结

这个实战项目展示了：

1. ✅ **现代 Rust Web 开发**: Axum + SQLx + Tokio
2. ✅ **清晰的架构**: 三层架构，职责分离
3. ✅ **类型安全**: 编译时检查，避免运行时错误
4. ✅ **异步高性能**: 充分利用 Rust 异步特性
5. ✅ **完整的 ORM/DAL**: Repository 模式实现
6. ✅ **统一错误处理**: 自定义错误类型
7. ✅ **生产级特性**: 日志、中间件、CORS
8. ✅ **可扩展设计**: 易于添加新功能

这是一个可以直接用于生产环境的项目模板！
